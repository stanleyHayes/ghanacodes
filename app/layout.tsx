import type{Metadata}from"next";import"./styles.css";export const metadata:Metadata={title:"GhanaCodes — Resolve Ghanaian identifiers safely",description:"Versioned, source-linked Ghana region and district code resolution.",metadataBase:new URL("https://codes.digitalghana.dev")};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
